import React from 'react'

export default function App() {
  return (
    <div className="app">
      <header className="app-header">
        <h1>Velvet Market — Curated Goods</h1>
        <p>Development build — welcome!</p>
      </header>
      <main>
        <p>Open the console and network tab to inspect API calls.</p>
      </main>
    </div>
  )
}
